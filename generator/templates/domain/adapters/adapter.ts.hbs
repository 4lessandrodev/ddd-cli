import { IAdapter, Result } from 'types-ddd';
import Model from '@modules/{{dashCase moduleName}}/domain/interfaces/model.interface';
import Aggregate from '@modules/{{dashCase moduleName}}/domain/aggregate';

export class {{pascalCase className}}ToDomainAdapter implements IAdapter<Model, Aggregate> {
	build(model: Model): Result<Aggregate> {
		// ... your code
		return Result.fail('Method not implemented');
	}
}

export default {{pascalCase className}}ToDomainAdapter;
