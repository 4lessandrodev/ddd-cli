import { ID } from 'types-ddd';
import Aggregate, { Props } from '@modules/{{dashCase moduleName}}/domain/aggregate';

describe('aggregate', () => {

	describe('success cases', () => {
		it('should create an aggregate with success', () => {

			const props: Props = {
				id: ID.create(),
				keyA: 'valid_value',
				keyB: 42
			};

			const result = Aggregate.create(props);
			expect(result.isOk()).toBeTruthy();
		});
	});
	
});
