import {{pascalCase className}}ValueObject from '@modules/{{dashCase moduleName}}/domain/value-object';

describe('value-object', () => {
	describe('success cases', () => {

		it('should create a valid instance', () => {
			const result = {{pascalCase className}}ValueObject.create('valid_value');
			expect(result.isOk()).toBeTruthy();
		});
	});

	describe('failure cases', () => {

		it('should fail if provide undefined value', () => {
			const result = {{pascalCase className}}ValueObject.create(undefined as unknown as string);
			expect(result.isFail()).toBeTruthy();
		});
	});
	
});
