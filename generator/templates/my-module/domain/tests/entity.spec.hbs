import Entity, { Props } from '@modules/my-module/domain/entity';

describe('entity', () => {

	describe('success cases', () => {
		it('should create an entity with success', () => {

			const props: Props = {
				id: ID.create(),
				keyA: 'valid_value',
				keyB: 42
			};

			const result = Entity.create(props);
			expect(result.isOk()).toBeTruly();
		});
	});

});

